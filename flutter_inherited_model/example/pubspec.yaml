name: example
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  flutter_inherited_model:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_inherited_model_builder:
    path: ../../flutter_inherited_model_builder

  flutter_lints: ^5.0.0
  build_runner: ^2.5.4
flutter:
  uses-material-design: true
